<div><h1>Booking information:</h1> </div>

<h2>General Information:</h2>
<div>
    <p>
        <strong>Booking number:</strong> <%= @booking.id %>
    </p>

    <p>
        <strong>Flight number:</strong> <%= @booking.flight_id %>
    </p>

    <p>
        <strong>Number of Passengers:</strong> <%= @booking.passengers.count %>
    </p>
</div>

<h2>Flight Information:</h2>

<div>
    <p>
        <strong>From Airport:</strong> <%= Airport.find(@flight.departure_airport_id).name_code %>
    </p>

    <p>
        <strong>To Airport:</strong> <%= Airport.find(@flight.arrival_airport_id).name_code %>
    </p>

    <p>
        <strong>Flight Duration:</strong> <%= @flight.duration %> min
    </p>
</div>

<h2>Passengers Information</h2>

<div>
    <% @passengers.each do |passenger| %>
        <ul>
            <li>
                <p>
                <strong>Name:</strong> <%= passenger.name %>
                </p>

                <p>
                <strong>Email:</strong> <%= passenger.email %>
                </p>
            </li>
        </ul>
    <% end %>
</div>

<%= link_to "[Home Page]", root_path %>
